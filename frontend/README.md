# wham frontend

Next.js 15 App Router frontend for wham. See the repo root `CLAUDE.md` for
project-wide context and `context/` for specs and conventions.

```bash
pnpm dev         # start the dev server
pnpm build       # production build
pnpm typecheck   # tsc --noEmit
pnpm lint        # eslint
```
